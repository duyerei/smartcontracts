# 付款管理功能说明

## 功能概述

在合同详情页面的补充协议下方，新增了付款管理列表，用于管理合同的付款记录。用户可以上传付款凭证（如发票、付款流程截图、付款凭证回单等），系统会自动识别付款时间和金额。

## 主要功能

### 1. 上传付款凭证
- 支持的文件格式：PDF、JPG、JPEG、PNG、DOC、DOCX
- 文件大小限制：最大 50MB
- 上传后系统自动识别付款时间和金额

### 2. 自动识别
- 使用OCR技术识别文件内容
- 使用LLM智能提取付款时间和金额
- 后台异步处理，不阻塞用户操作
- 识别过程中显示"识别中..."状态

### 3. 列表展示
- 表格形式展示所有付款记录
- 字段包括：付款资料、付款时间、付款金额、操作
- 支持查看附件（点击"查看附件"链接）
- 自动刷新：每5秒检查一次更新

### 4. 编辑功能
- 可以编辑付款说明、付款时间、付款金额
- 点击编辑按钮打开编辑对话框
- 保存后立即刷新列表

### 5. 删除功能
- 软删除，不会物理删除文件
- 删除前需要确认

## 技术实现

### 后端实现

#### 数据库模型
```python
class Payment(Base):
    __tablename__ = "payments"
    
    id = Column(Integer, primary_key=True, index=True)
    contract_id = Column(Integer, ForeignKey("contracts.id"), nullable=False)
    description = Column(String, nullable=False)
    payment_date = Column(DateTime, nullable=True)
    amount = Column(Float, nullable=True)
    file_path = Column(String, nullable=False)
    file_size = Column(Integer, nullable=False)
    is_deleted = Column(Boolean, default=False)
    created_at = Column(DateTime, default=datetime.utcnow)
```

#### API接口
- `POST /api/v1/payments/{contract_id}/upload` - 上传付款凭证
- `GET /api/v1/payments/{contract_id}/list` - 获取付款记录列表
- `GET /api/v1/payments/{payment_id}/download` - 下载/预览付款凭证
- `PATCH /api/v1/payments/{payment_id}` - 更新付款记录
- `DELETE /api/v1/payments/{payment_id}` - 删除付款记录

#### 文件存储
- 文件保存在 `backend/storage/payments/` 目录
- 文件命名格式：`{contract_id}_{uuid}{extension}`
- 支持多种文件格式

#### AI识别流程
1. 上传文件后立即返回，创建付款记录
2. 使用BackgroundTasks启动后台任务
3. OCR识别文件内容（PDF或图片）
4. LLM提取付款时间和金额
5. 更新数据库记录
6. 前端定时刷新自动获取最新数据

### 前端实现

#### 组件结构
- `PaymentList.tsx` - 付款管理列表组件
- 集成在 `ContractDetail.tsx` 页面中

#### 主要功能
- 文件上传对话框
- 上传进度提示
- 表格展示付款记录
- 编辑对话框
- 删除确认

#### 自动刷新
- 使用 `setInterval` 每5秒刷新一次
- 组件卸载时清理定时器

## 使用流程

1. 进入合同详情页面
2. 滚动到"付款管理"卡片
3. 点击"上传付款凭证"按钮
4. 选择付款凭证文件（发票、截图、回单等）
5. 系统自动上传并识别
6. 等待识别完成（显示"识别中..."）
7. 识别完成后自动显示付款时间和金额
8. 如需修改，点击"编辑"按钮
9. 如需删除，点击"删除"按钮

## 注意事项

1. 文件格式限制：仅支持 PDF、图片、DOC、DOCX
2. 文件大小限制：最大 50MB
3. AI识别准确率：取决于文件质量和内容清晰度
4. 识别时间：通常在5-10秒内完成
5. 如果识别失败，可以手动编辑补充信息

## 文件清单

### 后端文件
- `backend/app/database.py` - Payment模型定义
- `backend/app/routers/payments.py` - 付款管理路由
- `backend/app/routers/__init__.py` - 路由注册
- `backend/app/main.py` - 应用配置
- `backend/migrate_add_payments.py` - 数据库迁移脚本

### 前端文件
- `frontend/src/components/PaymentList.tsx` - 付款管理组件
- `frontend/src/pages/ContractDetail.tsx` - 合同详情页面（集成）

## 后续优化建议

1. 支持批量上传付款凭证
2. 支持导出付款记录为Excel
3. 添加付款统计图表
4. 支持付款提醒功能
5. 优化AI识别准确率
6. 添加付款审批流程
