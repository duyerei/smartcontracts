# 安全修复实施指南

本文档提供了完整的安全修复步骤和说明。

---

## 🚨 紧急修复步骤（立即执行）

### 步骤1：撤销已泄露的API密钥

**⚠️ 这是最紧急的步骤，必须立即执行！**

1. **百度OCR API密钥**
   - 登录百度智能云控制台
   - 进入"产品服务" > "人工智能" > "文字识别OCR"
   - 删除或重置当前API Key和Secret Key
   - 生成新的密钥对

2. **百度千帆大模型密钥**
   - 登录百度智能云控制台
   - 进入"千帆大模型平台"
   - 删除或重置当前API Key和Secret Key
   - 生成新的密钥对

3. **火山引擎ARK密钥**
   - 登录火山引擎控制台
   - 进入"ARK"服务
   - 删除或重置当前API Key
   - 生成新的密钥

### 步骤2：从Git仓库中删除敏感文件

```bash
# 1. 从Git缓存中删除.env文件
cd /path/to/project
git rm --cached backend/.env

# 2. 确保.gitignore已更新（已完成）
# 检查.gitignore文件是否包含以下内容：
# .env
# backend/.env
# /secure/

# 3. 提交更改
git add .gitignore
git commit -m "security: 移除敏感配置文件并更新.gitignore"

# 4. 如果已经推送到远程仓库，需要清理历史记录
# ⚠️ 警告：这会重写Git历史，需要团队协调
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch backend/.env" \
  --prune-empty --tag-name-filter cat -- --all

# 5. 强制推送（需要团队成员重新克隆仓库）
git push origin --force --all
```

### 步骤3：配置新的环境变量

```bash
# 1. 复制示例配置文件
cp backend/.env.example backend/.env

# 2. 编辑.env文件，填入新的API密钥
nano backend/.env

# 3. 生成强JWT密钥
python3 -c "import secrets; print(secrets.token_urlsafe(64))"

# 4. 将生成的密钥填入.env文件的JWT_SECRET_KEY字段
```

### 步骤4：安装新的依赖

```bash
cd backend
pip install -r requirements.txt
```

### 步骤5：重启服务

```bash
# 停止当前运行的服务
pkill -f "uvicorn"

# 启动服务
python run.py
```

### 步骤6：获取新的管理员密码

```bash
# 查看自动生成的管理员密码
cat secure/admin_password.txt

# 立即登录并修改密码
# 修改后删除此文件
rm secure/admin_password.txt
```

---

## ✅ 已完成的修复

### 1. API访问控制 ✅

所有合同管理接口已添加身份认证：

```python
from app.auth import get_current_user, User

@router.get("/contracts")
def list_contracts(
    current_user: User = Depends(get_current_user),  # ✅ 已添加
    db: Session = Depends(get_db)
):
    ...
```

**影响的接口**：
- ✅ GET /api/v1/contracts - 列出合同
- ✅ GET /api/v1/contracts/{id} - 查看合同详情
- ✅ POST /api/v1/contracts/upload - 上传合同
- ✅ PUT /api/v1/contracts/{id} - 更新合同
- ✅ DELETE /api/v1/contracts/{id} - 删除合同
- ✅ GET /api/v1/contracts/{id}/download - 下载合同
- ✅ GET /api/v1/contracts/{id}/analyze - 风险分析
- ✅ GET /api/v1/contracts/{id}/text - 获取合同文本
- ✅ POST /api/v1/contracts/{id}/reparse - 重新解析

### 2. CORS配置 ✅

已修复为仅允许配置的源：

```python
# 从环境变量读取允许的源
allowed_origins_str = os.getenv("ALLOWED_ORIGINS", "http://localhost:5173")
allowed_origins = [origin.strip() for origin in allowed_origins_str.split(",")]

app.add_middleware(
    CORSMiddleware,
    allow_origins=allowed_origins,  # ✅ 仅允许配置的源
    allow_credentials=True,
    allow_methods=["GET", "POST", "PUT", "DELETE", "PATCH"],  # ✅ 明确指定
    allow_headers=["Content-Type", "Authorization"],  # ✅ 明确指定
)
```

**配置方法**：
在 `backend/.env` 中设置：
```bash
ALLOWED_ORIGINS=http://localhost:5173,https://yourdomain.com
```

### 3. 路径遍历防护 ✅

文件路径验证已加强：

```python
def get_file_path(self, relative_path: str) -> Path:
    """获取文件路径，防止路径遍历攻击"""
    requested_path = (self.storage_path / relative_path).resolve()
    
    # 确保路径在storage_path内
    try:
        requested_path.relative_to(self.storage_path.resolve())
    except ValueError:
        raise ValueError("非法的文件路径")
    
    return requested_path
```

### 4. 文件上传安全 ✅

已添加多层验证：

1. ✅ 扩展名检查
2. ✅ 文件大小限制（50MB）
3. ✅ 文件头魔数验证
4. ✅ 安全的文件名生成（UUID）

```python
async def save_contract(self, file: UploadFile) -> tuple[str, str]:
    # 1. 检查扩展名
    # 2. 读取内容
    # 3. 检查大小
    # 4. 验证文件头
    # 5. 保存文件
```

### 5. Prompt注入防护 ✅

LLM输入已添加清理：

```python
def _sanitize_input(self, text: str) -> str:
    """清理输入文本，防止prompt注入攻击"""
    # 1. 限制长度
    # 2. 移除可疑指令
    # 3. 验证内容
```

### 6. 请求速率限制 ✅

已添加全局速率限制：

```python
from slowapi import Limiter

limiter = Limiter(key_func=get_remote_address, default_limits=["200/minute"])
app.state.limiter = limiter
```

**默认限制**：每分钟200次请求

**自定义限制**（可选）：
```python
from slowapi import Limiter
from fastapi import Request

@router.post("/upload")
@limiter.limit("10/minute")  # 上传接口限制更严格
async def upload_contract(request: Request, ...):
    ...
```

### 7. 强管理员密码 ✅

管理员密码已改为自动生成：

```python
def ensure_default_admin(db: Session):
    # 生成16字符随机密码
    random_password = secrets.token_urlsafe(16)
    # 保存到secure/admin_password.txt
```

---

## 📋 待实施的修复（建议）

### 1. 审计日志系统

创建审计日志模型：

```python
# backend/app/database.py
class AuditLog(Base):
    __tablename__ = "audit_logs"
    
    id = Column(Integer, primary_key=True)
    user_id = Column(Integer, index=True)
    username = Column(String(50))
    action = Column(String(50))  # create, update, delete, download, login
    resource_type = Column(String(50))  # contract, user
    resource_id = Column(Integer, nullable=True)
    ip_address = Column(String(50))
    user_agent = Column(String(500))
    details = Column(Text, nullable=True)  # JSON格式的详细信息
    created_at = Column(DateTime, default=datetime.now, index=True)
```

创建日志服务：

```python
# backend/app/services/audit_service.py
from app.database import AuditLog, SessionLocal
from fastapi import Request
import json

class AuditService:
    @staticmethod
    def log(
        user_id: int,
        username: str,
        action: str,
        resource_type: str,
        resource_id: int = None,
        details: dict = None,
        request: Request = None
    ):
        """记录审计日志"""
        db = SessionLocal()
        try:
            log = AuditLog(
                user_id=user_id,
                username=username,
                action=action,
                resource_type=resource_type,
                resource_id=resource_id,
                ip_address=request.client.host if request else None,
                user_agent=request.headers.get("user-agent", "") if request else None,
                details=json.dumps(details, ensure_ascii=False) if details else None
            )
            db.add(log)
            db.commit()
        except Exception as e:
            print(f"审计日志记录失败: {e}")
        finally:
            db.close()

audit_service = AuditService()
```

在关键操作中使用：

```python
# backend/app/routers/contracts.py
from app.services.audit_service import audit_service

@router.post("/upload")
async def upload_contract(
    file: UploadFile = File(...),
    current_user: User = Depends(get_current_user),
    request: Request = None,
    db: Session = Depends(get_db)
):
    # ... 上传逻辑
    
    # 记录审计日志
    audit_service.log(
        user_id=current_user.id,
        username=current_user.username,
        action="upload",
        resource_type="contract",
        resource_id=contract.id,
        details={"filename": file.filename, "size": len(content)},
        request=request
    )
    
    return response
```

### 2. 密码策略加强

```python
# backend/app/auth.py
import re

def validate_password(password: str) -> tuple[bool, str]:
    """验证密码强度"""
    if len(password) < 8:
        return False, "密码至少8位"
    
    if not re.search(r'[A-Z]', password):
        return False, "密码必须包含大写字母"
    
    if not re.search(r'[a-z]', password):
        return False, "密码必须包含小写字母"
    
    if not re.search(r'[0-9]', password):
        return False, "密码必须包含数字"
    
    if not re.search(r'[!@#$%^&*(),.?":{}|<>]', password):
        return False, "密码必须包含特殊字符"
    
    return True, ""

# 在创建/修改用户时使用
@router.post("/users")
def create_user(req: UserCreate, ...):
    valid, msg = validate_password(req.password)
    if not valid:
        raise HTTPException(status_code=400, detail=msg)
    ...
```

### 3. JWT Token刷新机制

```python
# backend/app/routers/auth.py
@router.post("/refresh", response_model=TokenResponse)
def refresh_token(
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """刷新访问令牌"""
    access_token = create_access_token(data={"sub": current_user.username})
    return TokenResponse(
        access_token=access_token,
        token_type="bearer",
        user=_user_dict(current_user)
    )
```

前端自动刷新：

```typescript
// frontend/src/lib/auth.ts
export async function refreshToken() {
  const token = localStorage.getItem('token')
  if (!token) return null
  
  try {
    const response = await fetch('/api/v1/auth/refresh', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${token}`
      }
    })
    
    if (response.ok) {
      const data = await response.json()
      localStorage.setItem('token', data.access_token)
      return data.access_token
    }
  } catch (error) {
    console.error('Token refresh failed:', error)
  }
  
  return null
}

// 在token即将过期时自动刷新（例如每4小时）
setInterval(refreshToken, 4 * 60 * 60 * 1000)
```

### 4. HTTPS强制（生产环境）

```python
# backend/app/main.py
from fastapi.middleware.httpsredirect import HTTPSRedirectMiddleware
import os

# 生产环境强制HTTPS
if os.getenv("ENVIRONMENT") == "production":
    app.add_middleware(HTTPSRedirectMiddleware)
```

配置Nginx反向代理：

```nginx
# /etc/nginx/sites-available/contract-system
server {
    listen 80;
    server_name yourdomain.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name yourdomain.com;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    location / {
        root /path/to/frontend/dist;
        try_files $uri $uri/ /index.html;
    }
}
```

### 5. 数据库迁移到PostgreSQL

```bash
# 1. 安装PostgreSQL
sudo apt-get install postgresql postgresql-contrib

# 2. 创建数据库和用户
sudo -u postgres psql
CREATE DATABASE contracts_db;
CREATE USER contracts_user WITH PASSWORD 'strong_password_here';
GRANT ALL PRIVILEGES ON DATABASE contracts_db TO contracts_user;
\q

# 3. 安装Python驱动
pip install psycopg2-binary

# 4. 更新.env配置
DATABASE_URL=postgresql://contracts_user:strong_password_here@localhost:5432/contracts_db

# 5. 迁移数据（如果需要）
# 导出SQLite数据
sqlite3 contracts.db .dump > dump.sql

# 转换并导入PostgreSQL
# 需要手动调整SQL语法差异
```

---

## 🔍 验证修复

### 1. 验证API认证

```bash
# 测试未认证访问（应该返回401）
curl http://localhost:8000/api/v1/contracts

# 测试认证访问（应该返回200）
TOKEN="your_jwt_token_here"
curl -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/v1/contracts
```

### 2. 验证CORS配置

```bash
# 测试不允许的源（应该被拒绝）
curl -H "Origin: http://evil.com" \
     -H "Access-Control-Request-Method: GET" \
     -X OPTIONS \
     http://localhost:8000/api/v1/contracts

# 测试允许的源（应该成功）
curl -H "Origin: http://localhost:5173" \
     -H "Access-Control-Request-Method: GET" \
     -X OPTIONS \
     http://localhost:8000/api/v1/contracts
```

### 3. 验证文件上传安全

```bash
# 测试上传非PDF文件（应该被拒绝）
echo "malicious content" > test.exe
curl -F "file=@test.exe" \
     -H "Authorization: Bearer $TOKEN" \
     http://localhost:8000/api/v1/contracts/upload

# 测试上传超大文件（应该被拒绝）
dd if=/dev/zero of=large.pdf bs=1M count=100
curl -F "file=@large.pdf" \
     -H "Authorization: Bearer $TOKEN" \
     http://localhost:8000/api/v1/contracts/upload
```

### 4. 验证速率限制

```bash
# 快速发送多个请求（应该被限制）
for i in {1..250}; do
  curl http://localhost:8000/api/v1/health &
done
wait
```

---

## 📊 安全检查清单

### 部署前检查

- [ ] 所有API密钥已重新生成
- [ ] .env文件已从Git仓库中删除
- [ ] .gitignore已正确配置
- [ ] JWT密钥已更换为强随机密钥
- [ ] 管理员密码已修改
- [ ] CORS配置已更新为生产域名
- [ ] 所有依赖已更新到最新版本
- [ ] 数据库已备份
- [ ] HTTPS已配置
- [ ] 防火墙规则已配置

### 运行时监控

- [ ] 审计日志正常记录
- [ ] 速率限制正常工作
- [ ] 文件上传验证正常
- [ ] API认证正常工作
- [ ] 错误日志正常输出
- [ ] 性能监控正常

---

## 📞 支持与反馈

如果在实施过程中遇到问题，请：

1. 检查日志文件：`backend/logs/`
2. 查看错误信息
3. 参考本文档的故障排除部分
4. 联系技术支持团队

---

**文档版本**: 1.0  
**最后更新**: 2026-03-02  
**维护人**: 技术团队
