# 补充协议预览修复说明

## 问题描述

1. 点击"查看附件"时提示"未能加载"
2. UI设计不够简洁，需要将文件名改为可点击的链接

## 修复内容

### 1. 前端修复（SupplementList.tsx 和 PaymentList.tsx）

#### UI改进
- 删除了"查看附件"字眼
- 将文件名/描述改为可点击的蓝色链接
- 点击文件名即可预览文件

**修改前：**
```tsx
<div>
  <p className="font-medium">{supplement.title}</p>
  <button onClick={() => handlePreview(supplement)} className="text-sm text-primary hover:underline">
    查看附件
  </button>
</div>
```

**修改后：**
```tsx
<button
  onClick={() => handlePreview(supplement)}
  className="font-medium text-primary hover:underline text-left"
>
  {supplement.title}
</button>
```

#### 预览功能增强
- 添加了更详细的错误处理
- 改进了 blob URL 的清理机制
- 添加了延迟清理，确保文件已加载

**修改前：**
```tsx
const handlePreview = async (supplement: Supplement) => {
  try {
    const response = await fetch(`/api/v1/supplements/${supplement.id}/download`, {
      headers: { 'Authorization': `Bearer ${token}` }
    })
    if (response.ok) {
      const blob = await response.blob()
      const url = window.URL.createObjectURL(blob)
      window.open(url, '_blank')
    }
  } catch (error) {
    alert('预览失败')
  }
}
```

**修改后：**
```tsx
const handlePreview = async (supplement: Supplement) => {
  try {
    const response = await fetch(`/api/v1/supplements/${supplement.id}/download`, {
      headers: { 'Authorization': `Bearer ${token}` }
    })
    if (response.ok) {
      const blob = await response.blob()
      const url = window.URL.createObjectURL(blob)
      
      // 在新窗口打开
      const newWindow = window.open(url, '_blank')
      
      // 清理URL（延迟清理，确保文件已加载）
      if (newWindow) {
        setTimeout(() => {
          window.URL.revokeObjectURL(url)
        }, 1000)
      }
    } else {
      const error = await response.json()
      alert(error.detail || '预览失败')
    }
  } catch (error) {
    console.error('预览失败:', error)
    alert('预览失败，请检查网络连接')
  }
}
```

### 2. 后端修复（supplements.py）

#### 问题
下载接口只设置了 `media_type="application/pdf"`，但补充协议可能是 DOC 或 DOCX 格式，导致浏览器无法正确识别文件类型。

#### 修复
根据文件扩展名动态设置正确的 media_type：

**修改前：**
```python
return FileResponse(
    path=str(file_path),
    media_type="application/pdf",
    filename=supplement.title
)
```

**修改后：**
```python
# 根据文件类型设置media_type
file_ext = os.path.splitext(supplement.file_path)[1].lower()
media_types = {
    '.pdf': 'application/pdf',
    '.doc': 'application/msword',
    '.docx': 'application/vnd.openxmlformats-officedocument.wordprocessingml.document'
}

media_type = media_types.get(file_ext, 'application/octet-stream')

return FileResponse(
    path=str(file_path),
    media_type=media_type,
    filename=supplement.title + file_ext
)
```

## 修复效果

### UI改进
- 界面更简洁，文件名直接显示为蓝色可点击链接
- 鼠标悬停时显示下划线，提示可点击
- 删除了冗余的"查看附件"文字

### 功能改进
- PDF 文件可以在浏览器中直接预览
- DOC/DOCX 文件会触发下载（浏览器不支持直接预览）
- 添加了详细的错误提示
- 改进了内存管理（blob URL 清理）

## 支持的文件格式

| 格式 | Media Type | 浏览器行为 |
|------|-----------|----------|
| PDF | application/pdf | 直接预览 |
| DOC | application/msword | 下载 |
| DOCX | application/vnd.openxmlformats-officedocument.wordprocessingml.document | 下载 |

## 测试步骤

1. 进入合同详情页面
2. 找到补充协议列表
3. 点击文件名（蓝色链接）
4. 验证：
   - PDF 文件应在新标签页中打开预览
   - DOC/DOCX 文件应触发下载
   - 如果出错，应显示友好的错误提示

## 相同修复应用于

- ✅ SupplementList.tsx（补充协议列表）
- ✅ PaymentList.tsx（付款管理列表）

## 注意事项

1. 浏览器对 DOC/DOCX 的支持有限，通常会触发下载而不是预览
2. PDF 文件可以在大多数现代浏览器中直接预览
3. 如果文件较大，预览可能需要一些时间
4. 确保后端文件路径正确，文件存在且可访问
