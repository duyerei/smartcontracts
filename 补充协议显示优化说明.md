# 补充协议和付款管理显示优化说明

## 优化内容

### 1. 文件名显示文件后缀

**问题**：文件名不显示后缀，用户无法直观看到文件类型

**解决方案**：
- 添加 `getFileExtension` 函数提取文件扩展名
- 在显示文件名时自动附加后缀

**实现代码**：
```tsx
const getFileExtension = (filePath: string) => {
  const match = filePath.match(/\.[^.]+$/)
  return match ? match[0] : ''
}

// 使用
{supplement.title}{getFileExtension(supplement.file_path)}
```

**效果**：
- 显示前：`绿盟网络信息安全设备新购及续保采购服务合同`
- 显示后：`绿盟网络信息安全设备新购及续保采购服务合同.pdf`

### 2. 识别超时显示"未识别"

**问题**：AI识别失败或超时时，一直显示"识别中..."，用户不知道是否识别失败

**解决方案**：
- 添加超时检测函数，判断创建时间是否超过2分钟
- 超时后显示"未识别"而不是"识别中..."

**实现代码**：
```tsx
// 判断是否超时（创建时间超过2分钟）
const isRecognitionTimeout = (createdAt: string) => {
  const created = new Date(createdAt).getTime()
  const now = Date.now()
  const twoMinutes = 2 * 60 * 1000
  return now - created > twoMinutes
}

// 使用
{supplement.signed_date ? (
  formatDate(supplement.signed_date)
) : isRecognitionTimeout(supplement.created_at) ? (
  <span className="text-xs text-muted-foreground">未识别</span>
) : (
  <span className="text-xs text-muted-foreground italic">识别中...</span>
)}
```

**效果**：
- 0-2分钟内：显示"识别中..."（斜体）
- 2分钟后：显示"未识别"（正常字体）
- 识别成功：显示实际值

## 应用范围

### 补充协议列表（SupplementList.tsx）
- ✅ 文件名显示后缀
- ✅ 签约时间超时显示"未识别"
- ✅ 金额超时显示"未识别"

### 付款管理列表（PaymentList.tsx）
- ✅ 文件名显示后缀
- ✅ 付款时间超时显示"未识别"
- ✅ 付款金额超时显示"未识别"

## 用户体验改进

### 1. 更清晰的文件类型识别
用户可以直接从文件名看到文件类型：
- `.pdf` - PDF文档
- `.doc` - Word文档（旧版）
- `.docx` - Word文档（新版）
- `.jpg`/`.jpeg`/`.png` - 图片文件

### 2. 更明确的识别状态
- **识别中**（斜体）：AI正在处理，请等待
- **未识别**（正常）：AI识别失败或超时，可以手动编辑
- **具体值**：AI识别成功

### 3. 自动刷新机制
- 每5秒自动刷新列表
- 识别完成后自动更新显示
- 超时后自动切换为"未识别"状态

## 技术细节

### 超时时间设置
```tsx
const twoMinutes = 2 * 60 * 1000  // 2分钟 = 120秒
```

**为什么选择2分钟？**
1. OCR识别通常在5-10秒内完成
2. LLM提取信息通常在10-20秒内完成
3. 加上网络延迟和重试，正常情况下1分钟内完成
4. 2分钟是一个合理的超时阈值

### 文件扩展名提取
```tsx
const match = filePath.match(/\.[^.]+$/)
```

**正则表达式说明**：
- `\.` - 匹配点号
- `[^.]+` - 匹配一个或多个非点号字符
- `$` - 匹配字符串结尾

**示例**：
- `supplements/27_abc123.pdf` → `.pdf`
- `payments/27_def456.docx` → `.docx`
- `payments/27_ghi789.jpg` → `.jpg`

## 测试场景

### 场景1：正常识别
1. 上传文件
2. 显示"识别中..."（斜体）
3. 5-20秒后识别完成
4. 显示实际值

### 场景2：识别超时
1. 上传文件
2. 显示"识别中..."（斜体）
3. 2分钟后仍未识别
4. 自动切换为"未识别"
5. 用户可以点击编辑手动填写

### 场景3：文件类型识别
1. 查看补充协议列表
2. 文件名显示完整后缀
3. 点击文件名预览或下载
4. PDF在浏览器中预览，DOC/DOCX触发下载

## 注意事项

1. **时区问题**：使用 `Date.now()` 和 `new Date(createdAt).getTime()` 都是UTC时间，不受时区影响
2. **自动刷新**：每5秒刷新一次，确保及时更新状态
3. **手动编辑**：识别失败或超时后，用户可以通过编辑按钮手动填写信息
4. **文件后缀**：后缀从 `file_path` 提取，不是从 `title` 提取，确保准确性

## 相关文件

- `frontend/src/components/SupplementList.tsx` - 补充协议列表组件
- `frontend/src/components/PaymentList.tsx` - 付款管理列表组件
- `backend/app/routers/supplements.py` - 补充协议后端路由
- `backend/app/routers/payments.py` - 付款管理后端路由
