# 管理员账号说明

## 当前状态

✅ **管理员账号已存在**

- **用户名**: `admin`
- **密码**: `admin123`
- **创建时间**: 2026-02-28 17:58:22

## 登录方法

### 方法1：直接使用现有账号（最简单）

```
访问前端登录页面
用户名: admin
密码: admin123
```

**登录后请立即修改密码！**

在前端界面：
1. 点击右上角头像
2. 选择"修改密码"
3. 输入旧密码: `admin123`
4. 输入新密码（至少8位，包含大小写字母、数字、特殊字符）
5. 确认修改

---

## 方法2：重置为强密码（推荐）

如果您想使用系统自动生成的强密码：

### 步骤1：运行重置脚本

```bash
cd backend
python reset_admin.py
```

### 步骤2：查看新密码

脚本会显示新密码，同时保存到 `secure/admin_password.txt`

```bash
cat secure/admin_password.txt
```

### 步骤3：使用新密码登录

```
用户名: admin
密码: （从文件中获取）
```

### 步骤4：删除密码文件

登录并修改密码后，删除密码文件：

```bash
rm secure/admin_password.txt
```

---

## 密码安全建议

### 强密码要求

- ✅ 至少8位字符
- ✅ 包含大写字母（A-Z）
- ✅ 包含小写字母（a-z）
- ✅ 包含数字（0-9）
- ✅ 包含特殊字符（!@#$%^&*等）

### 示例强密码

```
Admin@2026!Secure
Contract#Mgmt2026
SmartC0ntr@ct!
```

### 密码管理建议

1. **不要使用**：
   - ❌ 生日、电话号码
   - ❌ 常见单词（password, admin123）
   - ❌ 键盘序列（qwerty, 123456）

2. **建议使用**：
   - ✅ 密码管理器（1Password, LastPass, Bitwarden）
   - ✅ 随机生成的密码
   - ✅ 每个系统使用不同的密码

3. **定期更换**：
   - 每3-6个月更换一次密码
   - 怀疑泄露时立即更换

---

## 忘记密码怎么办？

如果忘记了管理员密码，可以通过以下方式重置：

### 方法1：使用重置脚本

```bash
cd backend
python reset_admin.py
```

### 方法2：直接修改数据库

```bash
cd backend
python -c "
from app.auth import hash_password
import sqlite3

new_password = 'YourNewPassword123!'
hashed = hash_password(new_password)

conn = sqlite3.connect('contracts.db')
cursor = conn.cursor()
cursor.execute('UPDATE users SET hashed_password=? WHERE username=\"admin\"', (hashed,))
conn.commit()
conn.close()

print('密码已重置为:', new_password)
"
```

---

## 常见问题

### Q1: 为什么找不到 admin_password.txt？

**A**: 因为您的管理员账号是在安全修复之前创建的，使用的是旧的弱密码 `admin123`。新的强密码机制只对新创建的账号生效。

**解决方案**：
- 使用 `admin123` 登录，然后修改密码
- 或运行 `reset_admin.py` 重置为强密码

### Q2: 登录时提示"用户名或密码错误"？

**A**: 可能的原因：
1. 密码输入错误（注意大小写）
2. 账号被禁用
3. 数据库损坏

**解决方案**：
```bash
# 检查账号状态
cd backend
python check_admin.py

# 如果账号被禁用，重新激活
python -c "
import sqlite3
conn = sqlite3.connect('contracts.db')
cursor = conn.cursor()
cursor.execute('UPDATE users SET is_active=1 WHERE username=\"admin\"')
conn.commit()
conn.close()
print('账号已激活')
"
```

### Q3: 可以创建多个管理员吗？

**A**: 可以。在前端"用户管理"页面，管理员可以创建新用户并设置为管理员角色。

### Q4: 如何查看所有用户？

**A**: 
```bash
cd backend
python -c "
import sqlite3
conn = sqlite3.connect('contracts.db')
cursor = conn.cursor()
cursor.execute('SELECT id, username, real_name, role, is_active FROM users')
for user in cursor.fetchall():
    print(f'ID: {user[0]}, 用户名: {user[1]}, 姓名: {user[2]}, 角色: {user[3]}, 状态: {user[4]}')
conn.close()
"
```

---

## 安全提醒

⚠️ **重要**：
1. 默认密码 `admin123` 是弱密码，必须立即修改
2. 不要在生产环境使用默认密码
3. 定期检查用户账号，删除不需要的账号
4. 启用审计日志，监控管理员操作
5. 使用强密码策略

---

**最后更新**: 2026-03-02  
**维护人**: 技术团队
