# 测试登录指南

## 当前服务状态

✅ **后端服务**: 运行在 `http://localhost:8000`  
✅ **前端服务**: 运行在 `http://localhost:3000`  
✅ **代理配置**: 前端 `/api` 请求会被代理到后端

## 登录信息

```
用户名: admin
密码: admin123
```

## 测试步骤

### 1. 访问前端登录页面

打开浏览器访问：
```
http://localhost:3000/login
```

### 2. 输入登录信息

- 用户名: `admin`
- 密码: `admin123`
- 点击"登录"按钮

### 3. 检查浏览器控制台

如果仍然出现错误，请：

1. 打开浏览器开发者工具（F12）
2. 切换到"Console"标签
3. 切换到"Network"标签
4. 尝试登录
5. 查看 `auth/login` 请求的详细信息

## 常见问题排查

### 问题1: 500 Internal Server Error

**可能原因**：
- 后端服务未启动
- 代理配置错误
- CORS配置问题

**解决方案**：
```bash
# 1. 检查后端是否运行
netstat -ano | findstr "8000"

# 2. 检查后端日志
# 查看后端终端输出

# 3. 测试后端API
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin&password=admin123"
```

### 问题2: Network Error / ECONNREFUSED

**可能原因**：
- 后端服务未启动
- 端口被占用
- 防火墙阻止

**解决方案**：
```bash
# 1. 重启后端服务
cd backend
python run.py

# 2. 重启前端服务
cd frontend
npm run dev
```

### 问题3: 401 Unauthorized

**可能原因**：
- 用户名或密码错误
- 账号被禁用

**解决方案**：
```bash
# 检查管理员账号
cd backend
python check_admin.py

# 如果需要，重置密码
python reset_admin.py
```

### 问题4: CORS Error

**可能原因**：
- CORS配置不正确
- 前端直接访问后端（绕过代理）

**解决方案**：

确保前端使用相对路径：
```typescript
// ✅ 正确 - 使用相对路径，会被代理
const API_BASE_URL = '/api/v1'

// ❌ 错误 - 直接访问后端，会有CORS问题
const API_BASE_URL = 'http://localhost:8000/api/v1'
```

## 直接测试后端API

如果前端登录失败，可以直接测试后端：

### 使用curl测试

```bash
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin&password=admin123"
```

### 使用Python测试

```bash
cd backend
python test_login.py
```

### 预期响应

```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer",
  "user": {
    "id": 1,
    "username": "admin",
    "real_name": "系统管理员",
    "department": "",
    "role": "admin",
    "is_active": true,
    "created_at": "2026-02-28T17:58:22.078441"
  }
}
```

## 浏览器测试

### 使用浏览器开发者工具测试

1. 打开浏览器访问 `http://localhost:3000`
2. 按F12打开开发者工具
3. 切换到"Console"标签
4. 执行以下代码：

```javascript
// 测试登录
fetch('/api/v1/auth/login', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/x-www-form-urlencoded'
  },
  body: 'username=admin&password=admin123'
})
.then(res => res.json())
.then(data => console.log('登录成功:', data))
.catch(err => console.error('登录失败:', err))
```

## 服务管理命令

### 启动服务

```bash
# 启动后端
cd backend
python run.py

# 启动前端（新终端）
cd frontend
npm run dev
```

### 停止服务

```bash
# 停止后端: Ctrl+C
# 停止前端: Ctrl+C
```

### 查看服务状态

```bash
# 查看端口占用
netstat -ano | findstr "8000"  # 后端
netstat -ano | findstr "3000"  # 前端
```

## 成功登录后

登录成功后，您应该：

1. ✅ 看到主页面
2. ✅ 右上角显示用户名"admin"
3. ✅ 可以访问合同管理功能
4. ✅ 可以访问用户管理功能（管理员权限）

## 首次登录建议

1. **立即修改密码**
   - 点击右上角头像
   - 选择"修改密码"
   - 输入新的强密码

2. **创建普通用户**
   - 进入"用户管理"页面
   - 创建日常使用的普通用户账号
   - 管理员账号仅用于管理操作

3. **测试功能**
   - 上传一份测试合同
   - 查看AI解析结果
   - 测试风险分析功能

---

**提示**: 如果问题仍然存在，请提供：
1. 浏览器控制台的完整错误信息
2. 后端终端的日志输出
3. Network标签中的请求详情
